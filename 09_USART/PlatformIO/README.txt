USARTSketch

This sketch demonstrates how to use the LibPrintf library to allow Arduino sketches to use the printf() function rather than Serial.print() etc. 

There are no Fritzing project files as the sketch only uses the Serial Monitor.
