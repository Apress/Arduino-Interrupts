TWI_Read

This sketch polls the TWI system to determine if data have been read. It requests two bytes from an LM75A temperature sensor.

It does not use interrupts.

A Fritzing project file and an exported image file exist for this project.
