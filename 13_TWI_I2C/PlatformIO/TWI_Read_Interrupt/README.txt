TWI_Read_Interrupt

This sketch uses the TWI interrupt to determine if data have been read. It requests two bytes from an LM75A temperature sensor.

A Fritzing project file and an exported image file exist for this project.
